# v2 → v3 clarification

Applied Grok's three blocking edits verbatim (grok_acceptance.md):
1. Explicitly distinguish excluded navigation-solution algorithms from in-scope explainable composite confidence and triage.
2. Replace ambiguous protocol phrase "Source-independent navigation algorithms" with new ownship-fix/navigation-solution algorithms; retain conditional distinct-family cross-checks.
3. Explicitly label ECDIS-inspired layout as UX familiarity, without standards/certification claim.

No gate, sample count, security control, latency, airgap or personnel scope changed. Grok states acceptance is unconditional once B1–B3 applied equivalently. These changes preserve the meaning of Claude B1 and clarify the intended scope rather than expanding it. Optional Grok N1/N2 not added to preserve page space; detailed controls and non-reliability language already in protocol.
